[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "cloudwright"
version = "0.1.0"
description = "Architecture intelligence for cloud engineers — design, cost, compare, export"
requires-python = ">=3.12"
license = "MIT"
dependencies = [
    "pydantic==2.11.7",
    "pyyaml==6.0.2",
    "anthropic==0.58.2",
    "openai==2.24.0",
]

[project.optional-dependencies]
cli = ["cloudwright-cli"]
web = ["cloudwright-web"]

[tool.hatch.build.targets.wheel]
packages = ["cloudwright"]
